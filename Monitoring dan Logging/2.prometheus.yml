global:
  scrape_interval: 10s # Prometheus akan mengambil data setiap 10 detik sekali

scrape_configs:
  - job_name: 'heart-disease-api-monitoring'
    static_configs:
      - targets: ['localhost:8001'] # Mengarah ke port Python exporter kita di atas